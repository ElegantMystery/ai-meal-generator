spring:
  application:
    name: backend

  profiles:
    active: dev
  
  session:
    cookie:
      sameSite: none
      secure: false

  datasource:
    url: jdbc:postgresql://localhost:5432/mealgen
    username: meal_user
    password: 236810
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none  # Flyway handles all schema management. Use 'validate' if you want Hibernate to check schema matches entities.
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true  # Creates baseline if schema history table doesn't exist
    validate-on-migrate: true  # Validates migrations before applying

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "446392270628-r2vjchlfn4u43aphqg3veke1ktvbe7q7.apps.googleusercontent.com"
            client-secret: "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"
            scope:
              - openid
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          google:
            issuer-uri: https://accounts.google.com

server:
  port: 8080

mealgen:
  rag:  
    base-url: http://localhost:8000
    secret: dev-secret

